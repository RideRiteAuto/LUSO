{
  "name": "@nevora/viewer-threejs",
  "version": "0.4.0",
  "private": true,
  "description": "Three.js inspection tool for Nevora World Compiler output. Not the game -- a debug viewer that only ever reads generator output files.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "tsx --test src/**/*.test.ts",
    "textures:ktx2": "node scripts/prepare-terrain-textures.mjs",
    "build:artifact": "node scripts/build-artifact.mjs"
  },
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@types/three": "^0.184.0",
    "esbuild": "^0.28.2",
    "tsx": "^4.20.6",
    "typescript": "^5.6.3",
    "vite": "^8.2.1"
  }
}
